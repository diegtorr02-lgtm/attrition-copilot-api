{
  "openapi": "3.1.0",
  "info": {
    "title": "Attrition Copilot API",
    "version": "1.0.0",
    "description": "Minimal schema for GPT Actions import"
  },
  "servers": [
    { "url": "https://example.com" }
  ],
  "paths": {
    "/health": {
      "get": {
        "operationId": "health",
        "summary": "Health check",
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": { "type": "string", "example": "ok" }
                  },
                  "required": ["status"]
                }
              }
            }
          }
        }
      }
    }
  }
}
